// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://amandamwong.github.io',
  // If your repo is named something other than amandamwong.github.io,
  // add a base path like this:
  // base: '/your-repo-name',
});
