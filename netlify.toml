# netlify.toml — place in root of project (same level as package.json)

[build]
  command = "npm ci && npx @tinacms/cli@latest build && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"